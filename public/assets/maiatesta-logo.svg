<!--
  PLACEHOLDER — interim reconstruction, not the real source file.
  No transparent export of the real Maiatesta logo exists yet. This SVG is a
  hand-built approximation (rounded lime square, diagonal black cut, rounded
  black notch) so the site has a working transparent-background logo to ship
  with. Replace this file in place with the real vector/extracted asset when
  available — no code changes elsewhere are needed, the path is stable.
-->
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 420 100" role="img" aria-label="Maiatesta">
  <defs>
    <clipPath id="maiaMarkClip">
      <rect x="0" y="0" width="100" height="100" rx="26" />
    </clipPath>
  </defs>
  <g>
    <rect x="0" y="0" width="100" height="100" rx="26" fill="#c6d91b" />
    <g clip-path="url(#maiaMarkClip)">
      <path d="M64 0 L100 0 L100 22 L48 68 L26 68 L26 44 Z" fill="#0a0900" />
      <rect x="24" y="56" width="24" height="34" rx="9" fill="#0a0900" />
    </g>
  </g>
  <text x="118" y="66" font-family="'Futura','Avenir Next Condensed','Century Gothic',sans-serif" font-weight="800" font-size="52" letter-spacing="0.5">
    <tspan fill="#ffffff">MA</tspan><tspan fill="#c6d91b">IA</tspan><tspan fill="#ffffff">TESTA</tspan>
  </text>
</svg>
